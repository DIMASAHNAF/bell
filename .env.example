# Host binding (0.0.0.0 agar bisa diakses dari luar/IP publik atau lokal)
HOST=0.0.0.0

# Port aplikasi Express
PORT=3000

# Environment (development / production)
NODE_ENV=production

# Path database SQLite (sql.js)
DB_PATH=database.db
